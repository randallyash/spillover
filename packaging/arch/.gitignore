# makepkg output. The AUR repository holds the recipe only — PKGBUILD, .SRCINFO
# and this file — because the release archive is downloaded when a user builds.
pkg/
src/
*.pkg.tar.*
*.tar.xz
*.tar.gz
